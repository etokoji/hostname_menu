import SwiftUI

struct MenuView: View {
    var body: some View {
        Text("Hello, Swift!")
    }
}

struct MenuView_Previews: PreviewProvider {
    static var previews: some View {
        MenuView()
    }
}
